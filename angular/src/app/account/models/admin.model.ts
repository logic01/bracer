export class Admin {
    public emailAddress: string;
    public firstName: string;
    public lastName: string;
    public initials: string; // todo - requested to be username but maybe emailaddress would be better?
}
